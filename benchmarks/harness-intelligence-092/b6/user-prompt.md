# B6 user request: CLI backend then UI observation

For the same absolute project and task, start or reuse the backend from the
CLI while no browser is open. Only after the CLI operation, open the UI and
verify that it observes the same canonical task/index and status. Record the
returned URL, lock/reuse behavior, raw backend and UI observations, canonical
state artifact/hash, and a visual UI artifact.

Use the installed Harness controls and UI as an observer/attach surface, not a
second task truth. Preserve raw CLI/API/browser evidence. Do not read the
development repository, scorer, prefilled result, or sealed holdout.
